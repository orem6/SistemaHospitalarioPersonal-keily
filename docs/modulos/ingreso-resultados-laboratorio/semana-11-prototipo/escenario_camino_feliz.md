# Evidencia: camino feliz

1. En **Resultados pendientes**, seleccione **Capturar resultado** para `BC-EVD-000002`.
2. Ingrese `13.5`; la unidad canonica `g/dL` es de solo lectura.
3. Seleccione **Guardar resultado** y compruebe el estado `Resultado ingresado como version 1`.
4. Abra Historial, inicie Correccion, indique `15.1` y el motivo `Error de transcripcion del analizador.`.
5. Confirme: el resumen muestra V1, V2, unidad y motivo antes de la escritura simulada.

El flujo representa `POST` exitoso (`201`) y la regla append-only del contrato; no afirma validacion clinica ni muestra datos de paciente.
