# Evidencia movil

Las capturas reales se guardan en `evidencia/capturas/` con viewport `390x844`:

- `movil-camino-feliz.png`: `?vista=exito`.
- `movil-error-critico.png`: `?vista=incierto`.

Se verifico visualmente el breakpoint movil: una columna, acciones a ancho disponible y sin scroll horizontal en los estados capturados.
